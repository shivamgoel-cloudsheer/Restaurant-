/** @type {import('next').NextConfig} */
const nextConfig = {
  // Images are plain <img>/CSS backgrounds in this scaffold so they always render.
  // When you swap in real photography, consider migrating to next/image and
  // adding your CDN host under images.remotePatterns here.
};

export default nextConfig;
