/**
 * Animated gold scalloped band - a nod to the lace divider above the Monkey Bar
 * footer. Pure CSS; the scallop pattern drifts sideways forever.
 */
export default function ScallopDivider() {
  return <div className="scallop" aria-hidden />;
}
